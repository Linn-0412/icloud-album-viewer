{
  "name": "icloud-album-viewer",
  "version": "0.1.0",
  "private": true,
  "description": "Lightweight chronological viewer for public iCloud shared album links.",
  "scripts": {
    "start": "node server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "nodemailer": "^10.0.0"
  }
}
